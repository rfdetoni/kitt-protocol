[package]
name = "kitt-protocol"
version = "0.1.1"
edition = "2024"
rust-version = "1.85"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
serde_json = "1"
